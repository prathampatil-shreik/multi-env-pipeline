# ============================================================
# RETIRED — DO NOT USE
# This workflow has been superseded by multi-env.yml
# It contained incorrect cluster/service names and used
# --force-new-deployment instead of proper image promotion.
# ============================================================
name: RETIRED - Old Pipeline (Do Not Use)

on:
  workflow_dispatch:

jobs:
  notice:
    name: This workflow is retired
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: |
          echo "This workflow is retired."
          echo "Use: TaskFlow Multi-Environment Pipeline (multi-env.yml)"
          exit 1
